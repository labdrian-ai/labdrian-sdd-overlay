version: "1"
skills:
  - id: sdd-spec
    path: sdd-spec
    source:
      type: core
      upstream:
        owner: gentleman-programming
    install:
      defaultScope: global
      targets:
        - claude
    lifecycle:
      updateStrategy: vendor-merge
  - id: sdd-spec
    path: sdd-spec-duplicate
    source:
      type: custom
    install:
      defaultScope: global
      targets:
        - claude
    lifecycle:
      updateStrategy: overlay-only
