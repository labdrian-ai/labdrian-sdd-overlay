# Tasks: Procedural Memory Lifecycle (draft, register, promote, revise, retire)

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Measured multipliers (2026-09-19) | Computed from `git show --numstat --format="" <commit>` on the two merged squash commits, excluding generated `review-receipts/*.json`: slice 1a (`0aab02b`, PR #353) totals 1466 insertions / 15 deletions across `lint.go` (632/0), `lint_test.go` (775/0), `codex-smoke.md` (40/0), `tasks.md` (19/15) — first-apply/forecast = 1086/350 ≈ **3.10x** (first-apply figures per the size-exception row below), delivered/forecast = 1481/350 ≈ **4.23x**. Slice 1b (`20c14e9`, PR #355) totals 797 insertions / 64 deletions across `main.go`, `lint.go`, `lint_cli.go`, `lint_cli_test.go`, `lint_test.go`, `skills.go`, `skills_test.go`, `style_guide_contract_test.go`, `tasks.md`, and the four skill-doc files — first-apply/forecast = 652/300 ≈ **2.17x**, delivered/forecast = 861/300 ≈ **2.87x**. Averaged: **first-apply ≈2.6x forecast**, **delivered ≈3.5x forecast**. A third, more stable ratio falls out of the same numbers: delivered/first-apply ≈ 1481/1086 ≈1.36x for 1a and 861/652 ≈1.32x for 1b — i.e. each 4-lens review has so far produced **one correction round adding ~30-35% on top of the first-apply size**, consistent with the "one correction round ≈25-35% of the slice" pattern named in the design. Test-to-production ratio observed: 1a 775/632 ≈1.23x, 1b's new/changed test files (`lint_cli_test.go` 254 + `lint_test.go` delta 145) vs new/changed production (`lint_cli.go` 91 + `lint.go` delta 23 + `skills.go` delta 9 + `main.go` delta 9) ≈399/132 ≈3.0x (CLI slices lean test-heavy because flag-parsing and dispatch surfaces are thin but need wide table coverage) — both within or above the 1.2-1.5x guidance, so bottom-up estimates below use 1.2-1.5x for library-shaped code and up to 2-3x for thin-CLI-over-fat-test-matrix code (registration/rollback/retirement CLIs). |
| Revised forecast (2026-09-19) | See table below. Bottom-up per-slice line counts (from tasks.md task lists and design.md's File Changes / per-slice mapping / Testing Strategy rows), cross-checked against original-estimate × the measured multipliers above. Six of the eight remaining slices are now expected to need `size:exception` or a split; only 5 stays close to its original estimate. |
| Chained PRs recommended | Yes — a feature-branch chain, each gated on the previous slice merging into its base; only the tracker merges to `main`. **Owner-decided 2026-09-19**: 3a, 3b and 7a are split into sequential sub-slices (3a-i/3a-ii, 3b-i/3b-ii, 7a-i/7a-ii); slices 2, 4, 5, 6 and 7b keep `size:exception` and are not split. This grows the remaining-slice count from 8 to 11 |
| 400-line budget risk | High for 3a, 3b, 4, 6, 7a, 7b (all now forecast to exceed 400 delivered lines); low for 2 and 5. Root cause confirmed by the multiplier measurement: the original 7→10-slice per-slice estimates in the design predate any real apply data and under-counted RED-phase boundary-table density (matches the 1a pattern) and correction-round overhead (~30-35%). **Resolved 2026-09-19**: 3a, 3b and 7a mitigate this risk by splitting into sub-slices that each land near 400-500 delivered lines; 2, 4, 5, 6 and 7b mitigate it through owner-granted `size:exception` instead (see below) |
| Size exceptions | **Slice 1a: `size:exception` granted by the owner on 2026-09-18** — corrected true figures, recomputed against base branch `feat/procedural-memory-lifecycle` (`git show --numstat` on the feature commit `f53653b`, excluding the docs-only receipt commit `c3969f3`): 1071 insertions / 15 deletions (`lint.go` +544/-0, `lint_test.go` +470/-0, `codex-smoke.md` +40/-0, `tasks.md` +17/-15) against a ~350 forecast. Reason: one cohesive 11-rule table with per-rule boundary tests; the only coherent split (hard vs advisory rules) leaves both halves near 500 lines. Correction commit (review-9ea42239d8e2080b advisories, 1a.6): 295 insertions / 23 deletions across `engine/skills/lint.go`, `engine/skills/lint_test.go`, `openspec/changes/procedural-memory-lifecycle/tasks.md`, `openspec/changes/procedural-memory-lifecycle/codex-smoke.md` (`git diff --stat` against `c3969f3`). **Slice 1b: `size:exception` granted by the owner on 2026-09-18** — 592 insertions / 60 deletions against the tracker (about 115 from the 1b.0 carry-forward fixes, about 405 for the CLI, its tests and the style-guide drift test, about 130 for docs and SKILL.md edits); the slice itself exceeded its ~300 forecast. **Slices 2, 4, 5, 6 and 7b: `size:exception` granted by the owner on 2026-09-19**, using the revised delivered estimates from the forecast table below — Slice 2 (`drafting-contract`) ~505; Slice 4 (`project-register-cli`) ~930; Slice 5 (`global-promotion`) ~465; Slice 6 (`revision`) ~850; Slice 7b (`retirement-detector`) ~1197. Reason for each is the cohesive-unit pattern already used for 1a/1b (test-and-content or detector-and-fixture-matrix cannot be separated without cutting a test from the code it verifies) |
| Decision needed before apply | **Resolved 2026-09-18** — owner chose `chain_strategy: feature-branch-chain`. A tracker branch holds the feature; PR 1 targets the tracker; each later PR targets the immediately previous slice branch; only the tracker merges to `main`, after PR 13. Slices remain strictly one at a time (apply → review → PR → merge into its base). `delivery_strategy: auto-chain`. **Resolved 2026-09-19** — owner granted `size:exception` for slices 2, 4, 5, 6 and 7b (no split), and approved splitting 3a, 3b and 7a into sequential sub-slices (3a-i/3a-ii, 3b-i/3b-ii, 7a-i/7a-ii), each its own PR in the chain. The chain is now: 1a (done), 1b (done), 2, 3a-i, 3a-ii, 3b-i, 3b-ii, 4, 5, 6, 7a-i, 7a-ii, 7b — 11 remaining PRs, 13 total |
| Reliability-review deltas (2026-09-18) | The reliability lens's approved, non-blocking findings added: one RED test each to 3b (rollback-of-rollback failure, task 3b.5a) and 7a (retirement rollback and rollback-of-rollback failure), plus non-Go acceptance-checklist cases to 2.4 (lesson-shape rejection) and 4.8 (staged-set mismatch, merge/rebase in progress, subdirectory/nested-root, hook-rewrite, second-registration crash recovery). These are already reflected in the current task lists for 2, 3b, 4 and 7a and are folded into the bottom-up estimates below (they are not an additional delta on top of the revised forecast) |

### Revised Review Workload Forecast (remaining slices 2, 3a-i, 3a-ii, 3b-i, 3b-ii, 4, 5, 6, 7a-i, 7a-ii, 7b)

| Slice | Original estimate | Revised first-apply estimate | Revised delivered estimate (incl. one correction round) | Over 400 (delivered)? | Recommendation |
|-------|-------------------|-------------------------------|-----------------------------------------------------------|------------------------|------------------|
| 2 — `drafting-contract` | ~250 | ~380 (contract-test extension for sections 7-10 ~150 lines + markdown sections 7-10 in `procedural-candidate-detection.md` ~200 lines + doc/tasks edits) | ~505 | Yes | `size:exception` (owner-granted 2026-09-19) — the contract test and the doc sections it verifies are one cohesive unit (same pattern as 1a's rule table); no split keeps both halves near 400 without separating a test from the content it asserts |
| 3a-i — `project-lock-format` | (part of ~350) | ~330 (`ParseProjectLock`/`SerializeProjectLock`/`StampProvenance` in `project_lock.go` ~150 + round-trip/unknown-field/version/provenance-table tests ~180) | ~430 | Marginal, yes | split (owner-decided 2026-09-19) — format half of `project-lock`, split from ownership along a coherent format-vs-ownership boundary; keeps its own tests |
| 3a-ii — `project-lock-ownership` | (part of ~350) | ~335 (`HashSkill`/`EvaluateOwnership`/`ValidateCandidateKey` in `project_lock.go` ~130 + tests across `EvaluateOwnership`'s 5 reasons and `ValidateCandidateKey`'s 4 shapes ~170 + import-allowlist widening) | ~435 | Marginal, yes | split (owner-decided 2026-09-19) — ownership half of `project-lock`; keeps its own tests |
| 3b-i — `project-register-write` | (part of ~370) | ~400 (pathguard extraction ~60 + `PlanProjectRegister`/happy-path write in `project_register.go` ~190 + non-rollback refusal tests of 3b.4 ~150) | ~520 | Yes | split (owner-decided 2026-09-19) — write half of `project-register-core`; write-path and rollback-path each carry their own tests |
| 3b-ii — `project-register-rollback` | (part of ~370) | ~440 (`ExecuteProjectPlan` stage/commit/rollback in `project_register.go` ~190 + injected-failure matrix + 3b.5a rollback-of-rollback tests ~250) | ~570 | Yes | split (owner-decided 2026-09-19) — rollback half of `project-register-core`; already flagged as the fallback split in the prior forecast row |
| 4 — `project-register-cli` | ~310 | ~700 (CLI flags/dispatch ~150 + CLI tests incl. carry-forward edge cases from review-c4c6f452b3ecc485 ~250 + section 11 procedure doc ~180 + its contract-test extension ~120) | ~930 | Yes | `size:exception` (owner-granted 2026-09-19) — implementation, the procedure doc and the contract test that verifies it are one cohesive delivery unit; not split |
| 5 — `global-promotion` | ~200 | ~350 (lint-gate insertion in `AddCore` ~50 + extended `lifecycle_test.go` scenarios ~150 + section 12 doc ~130 + carry-forward existing-test-fixture updates ~20) | ~465 | Marginal, yes | `size:exception` (owner-granted 2026-09-19) — smallest remaining slice; the lint gate and its one procedure-doc section are too tightly coupled to split without separating a two-line code change from the doc paragraph explaining it |
| 6 — `revision` | ~300 | ~640 (`project-revise` planning/execution reusing 3b's patterns ~200 + CLI/`project-status` wiring ~70 + tests across ownership-gate/backup-restore scenarios ~250 + section 13 doc ~110) | ~850 | Yes | `size:exception` (owner-granted 2026-09-19) — the revise engine, its CLI wiring and its one procedure-doc section stay together; not split |
| 7a-i — `retirement-engine-core` | (part of ~250) | ~390 (`project-retire` planning/execution + `AbsorbedInto` verification in `project_register.go` ~180 + tests incl. the reliability-review rollback-of-rollback addition ~210) | ~490 | Yes | split (owner-decided 2026-09-19) — core retirement mechanics and rollback, split from CLI/docs |
| 7a-ii — `retirement-cli-and-docs` | (part of ~250) | ~330 (CLI/`project-status` wiring ~60 + `project-status` supersession test ~50 + section 14 doc ~110 + doc/tasks edits ~110) | ~415 | Marginal, yes | split (owner-decided 2026-09-19) — CLI wiring, `project-status` supersession reporting and section 14 doc |
| 7b — `retirement-detector` | ~350 | ~900 (`staleness.go` extension ~90 + new `skillstale.go` detector ~230 + CLI subcommand/`main.go` wiring ~70 + cross-module pinning test ~30 + tests: `staleness_test.go` extension ~100, new `skillstale_test.go` with fixture lock/git-repo/Engram-DB scenarios ~380) | ~1197 | Yes, substantially | `size:exception` (owner-granted 2026-09-19) — the detector, its git-repo/Engram-DB fixtures and its per-scenario test matrix (removed/moved/unresolved-command/quiet/clean) are one cohesive unit like 1a's rule table; the only candidate boundary (staleness extraction vs the skillstale detector that consumes it) leaves the detector half at ~700-800, still 2x over budget, so it fails the "brings each part meaningfully closer to 400" bar and is not recommended |

Ten sequential slices were originally planned instead of the proposal's seven because revised decision (e) and the item-30 prerequisite gate add fixed per-slice overhead (contract-test wiring, target-table drift tests, the Codex smoke record) that the proposal's coarser 7-slice estimate did not itemize; the design's slice-mapping table is authoritative and is reproduced task-by-task below. The owner resolved the 2026-09-19 decision by splitting 3a, 3b and 7a into sequential sub-slices (3a-i/3a-ii, 3b-i/3b-ii, 7a-i/7a-ii, each its own PR in the chain) and granting `size:exception` to 2, 4, 5, 6 and 7b instead of splitting them; the phase headers and task IDs below now reflect that decision.

## Phase 0: Prerequisite Gate (blocks every slice below)

- [x] 0.1 Verify `main` at `engine/skills/match.go` exports `MatchCandidate` (item 30, phases 2-4)
- [x] 0.2 Verify `openspec/changes/archive/2026-09-18-procedural-candidate-detection/tasks.md` (read-only) has every task in phases 2-4 (2.x, 3.x, 4.x) checked `[x]` (item 30 was archived 2026-09-18, PR #351)
- [x] 0.3 Verify `skills/_shared/procedural-candidate-detection.md` on `main` contains sections 4, 5 and 6 (emission decision table, `Status` latch, `MatchCandidate`/rejection fields)
- [x] 0.4 If any of 0.1-0.3 fails, STOP: report `status: blocked`, name the missing item, and do not start slice 1a (none failed; proceeding to slice 1a)
- [x] 0.5 Verify `engine/skills/zero_fetch_test.go`'s `allowedImports` already includes `path` (item 30's own widening for `MatchCandidate`'s `path.Base`); slice 3a's widening (§3a.3) is additive on top of this, not a replacement

## Phase 1a: `skill-lint-core` (PR 1, ~350 lines)

Item-30 symbols consumed: none directly — this slice only depends on the Phase 0 gate having passed.

- [x] 1a.1 Codex smoke test record (non-Go; requires explicit owner authorization at apply time for the `codex exec` remote probe — design §"Fixed two-target set", steps 1-4)
  - [x] 1a.1.1 At apply time, ask the owner for explicit authorization: destination (Codex provider), operation (≤2 probe prompts + 1 control, no repository content sent), credential (existing Codex login session)
  - [x] 1a.1.2 If authorized: run the procedure in a fresh `mktemp -d` outside every repo and outside the user's home .agents directory — record `codex --version` (expect `0.148.0`, else verdict `INCONCLUSIVE`), `codex --help`, `codex exec --help`; `git init` the temp dir; create `.agents/skills/labdrian-smoke-probe/SKILL.md` with the fresh-nonce body; treatment run (`codex exec --cd <tmp> "..."`); control run (skill moved to `.agents/not-skills/...`)
  - [x] 1a.1.3 (verdict PASS via a disclosed probe-2 variant with the nonce in the description; probe 1 INCONCLUSIVE on a host sandbox limitation — see codex-smoke.md. Scoped claim per review-9ea42239d8e2080b advisory R3-smoke-verdict-scope: Codex discovery of `.agents/skills/<id>/` is verified; loading the skill body is unverified on this host) Determine verdict per design rule: `PASS` (nonce in treatment only), `FAIL` (nonce absent from treatment), `INCONCLUSIVE` (auth/network/trust failure or nonce leaks into control)
  - [x] 1a.1.4 (not applicable: authorization was granted) If not authorized: record verdict `UNVERIFIED` and continue without blocking this slice
  - [x] 1a.1.5 Write `openspec/changes/procedural-memory-lifecycle/codex-smoke.md`: date, version, authorization status, exact commands, outputs (absolute home paths redacted to a tilde), verdict
- [x] 1a.2 RED `engine/skills/lint_test.go`: boundary table for every hard rule (`frontmatter-fence` missing/unclosed; each of `name`/`description`/`license`/`metadata.author`/`metadata.version` missing or empty; `description-one-line` with a block scalar and with an indented continuation; `description-max` at 250/251 runes; `body-hard-budget` at 4000/4001 bytes) and every advisory (`description-should` at 160/161; `body-recommended` at 2800/2801; `section-order` in/out of canonical order with a missing heading allowed and an unknown heading ignored; `incident-log-shape` for an ISO date, an `engram:\d+` ref, and a PR/issue number, each independently; `banned-shell-utility` for each of `cat`/`grep`/`find`/`sed`/`ls` inline and fenced, plus a near-miss `catalog` that must NOT match; `home-path-leak` for a Linux home-directory prefix, a macOS Users prefix and a Windows Users prefix, each followed by a user name (exact fixture strings are in design.md, lint rule table; they are test data, not edit paths); determinism (two calls, byte-identical output); a clean fixture with zero hard errors and zero warnings
- [x] 1a.2a RED `engine/skills/lint_test.go`: `LintSkillFile` boundary tests for a missing fence and an unclosed fence, each surfaced as a hard `frontmatter-fence` error, plus a test proving `LintSkill` is never invoked when the split fails (observable via zero warnings and exactly one hard error on garbage input that would otherwise trigger many `required-fields` findings) (carry-forward, owner-accepted, review-42be59cf3243a7ff advisory R3-lintskillfile-coverage: this scenario moved from `LintSkill` to the new `LintSkillFile Composes SplitSkillFile and LintSkill` requirement in skill-lint/spec.md)
- [x] 1a.3 GREEN `engine/skills/lint.go`: `Severity`, `LintError`, `Warning`, `lintRules []lintRule` table, `SplitSkillFile` (frontmatter-fence check lives here), a line-based frontmatter subset reader (top-level `key: value`, one `metadata:` block with 2-space-indented pairs, quote stripping — separate from `parse.go`), `LintSkill(frontmatter, body string) (hard []error, warnings []Warning)`, `LintSkillFile(data []byte)`, `EstimateBodyTokens(body string) int = (len(body)+3)/4`, named constants `DescriptionMaxRunes=250`, `DescriptionShouldRunes=160`, `BodyHardTokens=1000`, `BodyRecommendedTokens=700`, `BytesPerTokenProxy=4`, `RenderLintRules() string`
- [x] 1a.4 REFACTOR: confirm `LintSkill`/`LintSkillFile` perform no filesystem/network/`os/exec` access and import stdlib only; confirm `RenderLintRules()` output renders every rule's numeric bound from the named constants (no hardcoded literals in the rendered text)
- [x] 1a.5 Verify: `cd engine && go test ./skills/...`
- [x] 1a.6 Correction commit addressing all 11 non-blocking advisories from review-9ea42239d8e2080b, with iterative RED/GREEN evidence per finding: R3-render-untested (`RenderLintRules` header/row-count/rule-id/numeric-constant tests), R3-render-table-pipe (escape `|` in rendered table cells), R2-rendered-summaries-duplicate-data (derive `description-one-line`/`section-order` summaries from `blockScalarIndicators`/`canonicalSections`), R4-bom-fence-false-hard-error (`SplitSkillFile` strips a leading UTF-8 BOM and trims trailing fence whitespace), R2-empty-description-flagged-multiline (empty `description:` now reports required-fields only, not description-one-line), R2-body-budget-test-literals (test-only: 4000/4001/2800/2801 derived from `BodyHardTokens`/`BodyRecommendedTokens`*`BytesPerTokenProxy`), R3-fenced-near-miss (added negative fenced/prose near-miss tests, no code change needed), R2-windows-fixture-literal-backslash-n (fixed the raw-string fixture to use a real newline), R2-stray-indent-comment-misleading (added a decision test for a one-space-indented metadata entry; fixed the misleading comment), R3-size-exception-count (recomputed true insertion/deletion figures against the base branch), R3-smoke-verdict-scope (narrowed the Codex smoke-test verdict claim to discovery-only)
- [x] 1a.7 Second correction commit addressing the 6 remaining advisories from review-6353816f626756f1, with iterative RED/GREEN evidence per finding: R3-empty-desc-continuation-reclassified (an empty `description:` tag line followed by an indented continuation is now classified `DescriptionMultiLine`, raising `description-one-line` and no longer a misleading `required-fields`, while an empty tag line with no continuation still reports `required-fields` only), R2-description-empty-dead-field (removed the unused `DescriptionEmpty` field; `checkRequiredFields`'s description presence check now reads `fm.Description != "" || fm.DescriptionMultiLine`), R2-indicator-test-vacuous and R3-desc-indicator-test-vacuous (rewrote the description-one-line indicator test to parse the rendered summary cell's parenthesized indicator list, extract its code-span tokens, unescape `\|`, and compare the exact set against a literal expected set independent of `blockScalarIndicators`; proved non-vacuity by temporarily dropping `>`/`|` from `blockScalarIndicators` and observing the test fail, then restored), R2-stray-indent-comment-overclaims (narrowed the stray-indent comment to name only the five required fields as diagnosed, and state that any other stray-indented key/value is dropped silently with no diagnostic), R2-transient-review-ids-in-comments (replaced every review-finding-ID/hash reference in test comments with a description of the invariant each test protects)

## Phase 1b: `skill-lint-surface` (PR 2, ~300 lines)

Item-30 symbols consumed: none.

- [x] 1b.0 Carry-forward fixes from review-b375153aa152604e (owner-accepted, apply-time): (1) `hasIndentedContinuation` (lint.go) no longer counts a whitespace-only continuation line as a genuine YAML continuation — RED `TestLintSkill_EmptyDescriptionWithWhitespaceOnlyLineYieldsRequiredFieldsOnly` failed against the old implementation, GREEN after requiring the line to carry non-whitespace content; (2) added edge-coverage tests: tab-indented continuation (`TestLintSkill_EmptyDescriptionWithTabContinuationYieldsDescriptionOneLine`), empty tag as the last frontmatter line (`TestLintSkill_EmptyDescriptionAsLastFrontmatterLineYieldsRequiredFieldsOnly`), and the whitespace-only line from (1); (3) fixed the doc comment above `TestRenderLintRules_DescriptionOneLineListsEveryBlockScalarIndicator` that incorrectly said `want` is compared against `blockScalarIndicators` — it is deliberately an independent literal set, for non-vacuity; (4) `splitTableRowCells` now drops only the leading/trailing empty cells produced by a row starting/ending with `|`, keeping a genuinely empty middle cell — RED `TestSplitTableRowCells_KeepsMiddleEmptyCell` failed against the old (drop-all-empty) implementation, GREEN after the fix
- [x] 1b.1 RED `engine/skills/lint_cli_test.go`: `lint <path>` exit 0 with warnings-only output; exit 1 on any hard error with the missing field named in output; missing/unreadable path handling; `lint --rules` output equals `RenderLintRules()` byte-for-byte; wrapper-injected trailing flags (`--registry`, `--manifest`, `--source-root`) are tolerated and ignored
- [x] 1b.2 GREEN `engine/skills/lint_cli.go`: `RenderLintCore` implementing `lint <path>` and `lint --rules`
- [x] 1b.3 GREEN `engine/skills/skills.go`, `engine/cmd/main.go`: dispatch the `lint` verb; usage strings
- [x] 1b.4 RED `engine/skills/style_guide_contract_test.go`: `TestSkillStyleGuideLintRulesInSync` (byte-identical between the generated-block markers and `RenderLintRules()`, printing the expected block on failure); `skill-improver`'s copy is byte-identical to `skill-creator`'s; none of `skills/skill-creator/SKILL.md`, `skills/skill-improver/SKILL.md`, `skills/skill-registry/SKILL.md` mentions `docs/skill-style-guide.md`; the "Frontmatter Rules"/"Body Budget" prose sections in the style guide contain no numeric restatement of 250/160/700/1000 outside the generated block
- [x] 1b.5 GREEN `skills/skill-creator/references/skill-style-guide.md`: insert the generated block between the BEGIN GENERATED skill-lint-rules and END GENERATED HTML-comment markers (exact marker text in design.md) (paste the output of `labdrian skills lint --rules`, run manually — no engine write path); strip the numeric restatements from prose, keeping the 180-450 token human-judgement guidance as prose
- [x] 1b.6 GREEN `skills/skill-improver/references/skill-style-guide.md`: byte-identical copy of the skill-creator file
- [x] 1b.7 GREEN `skills/skill-creator/SKILL.md`: drop "Inline Fallback Rules" numbers and the `docs/skill-style-guide.md` reference; document that it runs `labdrian skills lint`
- [x] 1b.8 GREEN `skills/skill-improver/SKILL.md`, `skills/skill-registry/SKILL.md`: drop the `docs/skill-style-guide.md` reference
- [x] 1b.9 REFACTOR: confirm no engine code path writes under `skills/` (print-and-paste only, per decision (a) alternatives-considered)
- [x] 1b.10 Verify: `cd engine && go test ./skills/... ./cmd/...`
- [x] 1b.11 Correction commit resolving review-b75e4a27b9494ff8 advisories: `lint` rejects a second positional argument or an unknown flag with exit 1 and a usage error naming the offending argument (R4-001, R2-silent-extra-args, R3-lint-extra-positional-ignored); `hasIndentedContinuation` skips whitespace-only lines and decides on the next non-blank line instead of stopping at the first whitespace-only line (R3-whitespace-continuation-false-negative); added a `SkillsCore("lint", ...)` dispatch-routing test (R3-lint-dispatch-untested); asserted the missing-path stderr diagnostic (R3-missing-path-stderr-unasserted); `extractGeneratedBlock` now trims exactly one leading/trailing newline, fails on missing/out-of-order markers via a shared `generatedBlockMarkerLocation` helper also reused by the numeric-source-drift test (R2-extract-block-comment-mismatch, R3-extract-block-trims-all-newlines); the restated-number regex is built from the `DescriptionMaxRunes`/`DescriptionShouldRunes`/`BodyRecommendedTokens`/`BodyHardTokens` lint constants instead of literals (R2-duplicated-numeric-bounds); the warnings-only fixture size is derived from those same constants via a named helper (R2-unexplained-test-constants); `skill-creator`/`skill-improver` `SKILL.md` document the manual style-guide fallback when the `labdrian` binary is unavailable (R4-002)

## Phase 2: `drafting-contract` (PR 3, ~250 lines, size:exception owner-granted 2026-09-19)

Item-30 symbols/sections consumed: sec 1 topic-key shapes, sec 2 record block, sec 4 emission table, sec 5 `Status` latch, sec 6 rejection fields and `MatchCandidate` call site (read-only reference, not modified).

- [x] 2.1 RED `engine/skills/procedural_candidate_contract_test.go` (extend, following the item-30 pattern): assert sections 7-10 appear verbatim — do-not-capture list (5 categories), lesson-shape rule (imperative + one why-clause, no ids/dates/PR numbers), `Disposition: new | extend:<skill-id>`, draft record topic-key shape `procedural/drafts/{kind}/{slug}` and its field block (`Candidate`, `Disposition`, `Status`, `ForRevision`, `Lint`, `History`, `Body`), extended `Status` vocabulary (`observing | emitted | rejected | drafted | registered | promoted | retired`), the full transition table (10 rows), the `History` line format and write rule (read-copy-append-upsert, prefix invariant), the new candidate-record fields (`Disposition`, `Draft`, `Registered`, `Promoted`, `OccurrencesSincePromotion`, `RetirementReason`, `AbsorbedInto`), and the runtime-targets table (`.claude/skills/`, `.agents/skills/`, Codex status cell, Pi trust note; asserts no row names `.pi/skills`)
- [x] 2.2 GREEN `skills/_shared/procedural-candidate-detection.md`: write sections 7-10 exactly as asserted in 2.1, sourced from design decisions (linked spec reqs: `procedural-skill-drafting` — Do-Not-Capture, Lesson Shape, Disposition, Draft Records Live Only in Engram, Extended Status Vocabulary, History Is Append-Only; `procedural-candidate-detection` delta — Do-Not-Capture List Gates Candidate Quality, Extended Status Vocabulary, Disposition Field, Append-Only History)
- [x] 2.3 REFACTOR: confirm sections 7-10 cross-reference item 30's sections 1-6 by name rather than duplicating their text
- [x] 2.4 Acceptance checklist (non-Go, executed during `sdd-verify`, per `procedural-skill-drafting`'s lesson-shape requirement): a Summary containing an `engram:<id>` reference is rejected before any draft record is saved; a Summary containing a literal date is rejected before any draft record is saved; a Summary containing a PR/issue number is rejected before any draft record is saved; a clean imperative-rule-plus-one-why-clause Summary passes and its draft record is saved; confirm the advisory `incident-log-shape` lint warning is a separate, non-blocking signal that never gates drafting
- [x] 2.5 Verify: `cd engine && go test ./skills/... ./cmd/...`
- [x] 2.6 Correction commit resolving review-77e824f1dcccfc4f advisories: `procedural-candidate-detection.md` section 9 now scopes the extended `Status` vocabulary to the candidate record and states the draft record's own, separate `Status` vocabulary (`open | registered | abandoned`) and how the two relate (R3-draft-status-vocabulary-contradiction), applied identically to `specs/procedural-candidate-detection/spec.md` and `specs/procedural-skill-drafting/spec.md`; the section 8 draft field-block example's outer fence widened from three to five backticks so it strictly outranks the inner four-backtick body fence and no longer closes early (R3-nested-fence-terminates-early), verified by a new `scanFences` CommonMark-rule test helper; the draft-field-block subtest in `procedural_candidate_contract_test.go` now anchors to the text between the section 8 and section 9 headings and to the field-block example itself, asserting field order (R3-draft-field-block-assertion-unanchored, non-vacuity proven by temporary deletion); the section 9 transition table now has its exact row count (10) pinned by a dedicated subtest (R3-transition-table-row-count-unasserted); the section 10 runtime-targets negative check now scans every cell of every row for the Pi skills directory, not only the first cell (R3-pi-skills-negative-check-first-column-only)

## Phase 3a-i: `project-lock-format` (PR 4, ~430 lines, split owner-decided 2026-09-19)

Item-30 symbols consumed: none directly — this sub-slice covers only the lock file format (parse/serialize/provenance stamping); `NormalizeSlug` and sec 1 key shapes are consumed in project-lock-ownership below.

- [x] 3a-i.1 RED `engine/skills/project_lock_test.go`: `ParseProjectLock`/`SerializeProjectLock` round-trip and `id`-sort determinism; unknown-field refusal (`DisallowUnknownFields`); `version != 1` refusal; missing file handled as empty by the caller (documented, not implemented here); `StampProvenance` table — insert into an existing `metadata:` block, replace an existing `author`/`provenance`/`candidate` line, preserve other metadata lines (e.g. `version`) byte-for-byte in original order, refusal on a missing `metadata:` block
- [x] 3a-i.2 GREEN `engine/skills/project_lock.go`: `ProceduralAuthor` constant (`"labdrian-overlay procedural"`), `ProjectLockRelPath = ".labdrian/procedural-skills.lock.json"`, `ProjectLock`/`ProjectLockEntry` types, `ParseProjectLock`, `SerializeProjectLock` (2-space indent, trailing newline, sorted by `id`), `StampProvenance`
- [x] 3a-i.3 GREEN `engine/skills/zero_fetch_test.go`: widen `allowedImports` by `encoding/json` (first needed here for `ParseProjectLock`/`SerializeProjectLock`; `crypto/sha256` and `encoding/hex` are widened in project-lock-ownership below, where `HashSkill` first needs them)
- [x] 3a-i.4 Verify: `cd engine && go vet ./... && go test ./skills/...`
- [x] 3a-i.5 Correction commit resolving review-70263a4dee1c98b7 advisories (22 findings, 9 defect groups): `StampProvenance` rejects an empty/control-character/non-plain-scalar `candidateKey` and double-quotes+escapes the emitted candidate value (R1-candidate-key-unescaped, R4-candidate-key-unvalidated-yaml, R3-candidate-key-unquoted, R2-candidate-value-unescaped); `ParseProjectLock` refuses trailing data after the decoded value (R4-lock-trailing-data-silently-dropped, R3-trailing-data-accepted) and duplicate skill ids (R4-unstable-sort-breaks-determinism, R3-unstable-sort-duplicate-ids, paired with `sort.SliceStable` in `SerializeProjectLock`); `SerializeProjectLock` normalizes a zero-value `Version` to 1 and refuses any other unsupported version (R4-serialize-emits-unreadable-version, R2-serialize-version-passthrough, R3-version-passthrough); `StampProvenance` refuses a `metadata:` line carrying an inline value (R4-metadata-inline-mapping-corrupts-output, R3-inline-metadata-mis-stamped); the stamped lines reuse the existing metadata block's detected indentation, falling back to two spaces for an empty block (R2-hardcoded-stamp-indent); direct-literal tests now pin `ProceduralAuthor` and `ProjectLockRelPath` (R3-constant-values-unproved), a golden-bytes test pins `SerializeProjectLock`'s exact indentation/key-order/trailing-newline (R3-weak-indent-assertion), and the provenance-replacement test anchors its assertions to the extracted metadata block (R2-unanchored-test-assertions); the unreachable `len(lines) == 0` disjunct is removed (R4-dead-empty-lines-guard, R2-dead-length-guard); the block-end scan and keep/drop filter now share one `classifyMetadataLine` helper (R2-duplicated-line-classification); the `ProceduralAuthor`/`ProjectLockRelPath` doc comments now cite `proposal.md:140` and `design.md:167` alongside the decision letters (R2-unsourced-decision-refs). R1-lock-targets-unvalidated (SUGGESTION) was deferred here to 3b-i.5a on the reasoning that no writer consumes `Targets` yet in this slice — see the scope correction on 3b-i.5a: that reasoning ignored the read side, and 3a-ii.6 fixes the traversal it let through.
- [x] 3a-i.6 Correction commit resolving review-24fc80ac3513305c advisories (4 findings, round 2): `SerializeProjectLock` now refuses duplicate skill ids, mirroring `ParseProjectLock`'s refusal, so a writer can never emit a lock nobody can read back (R3-duplicate-id-write-asymmetry; the test that had asserted the old duplicate-tolerant serialize behavior now asserts refusal instead, and a renamed stability test covers two distinct ids); `validateCandidateKey` now refuses an interior double quote or backslash outright (decision: reject in validation, keep `escapeYAMLDoubleQuoted` as defense in depth) with new refusal tests for both (R3-candidate-escape-unproved); the leading-space, trailing-space, interior `": "` and interior `" #"` refusal branches each get their own test asserting the specific error they return (R3-validation-branches-unexercised); the empty-metadata-block fallback test now asserts all three stamped lines' two-space indentation, not only the author line (R3-fallback-indent-partially-asserted).

## Phase 3a-ii: `project-lock-ownership` (PR 5, ~435 lines, split owner-decided 2026-09-19)

Item-30 symbols consumed: `NormalizeSlug`; sec 1 key shapes (in `ValidateCandidateKey`).

- [x] 3a-ii.0 Carry-forward correction group resolving review-d89971d41a526146 (previous slice's round-3 review, landed with this slice): `TestSerializeProjectLock_StableForNonDuplicateEntries` regains its reversed-input half, so order-independent serialization is proved by a byte comparison of two serializations rather than by a sorted-order index check `TestSerializeProjectLock_SortsByID` already made; `validateCandidateKey`'s interior double-quote and interior-backslash refusals are split into one branch and one message each (they had shared a single message, leaving the two tests indistinguishable) and both tests now pin their own message and assert the other branch's wording is absent; `escapeYAMLDoubleQuoted` is KEPT and gains a direct unit test (`TestEscapeYAMLDoubleQuoted_EscapesQuoteAndBackslash`, including the `a\"b` case proving backslashes are escaped before quotes) rather than being deleted — its branches are unreachable through `StampProvenance` only because validation now refuses both characters, and 3b-i.5's `PlanProjectRegister` is the next writer of candidate values, so removing the guard would also reverse the decision already recorded in 3a-i.6 and in its doc comment; `TestSerializeProjectLock_RefusesDuplicateIDs` now asserts the returned bytes are nil and that the message names both the defect and the duplicated id, with `TestParseProjectLock_DuplicateIDRefused` pinned symmetrically so the asymmetry the original finding was about is not left half closed
- [x] 3a-ii.1 RED `engine/skills/project_lock_test.go`: `HashSkill` known-vector test; `EvaluateOwnership` reasons — agent-owned, `hash-mismatch <path>`, `missing <path>`, `extra-entry <path>`, `not-in-lock`; `ValidateCandidateKey` — valid `repeated-success/<s>` and `failure-recovery/<s>/<s>` shapes, empty segment, non-normalized segment (fails `NormalizeSlug` idempotence)
- [x] 3a-ii.2 GREEN `engine/skills/project_lock.go`: `HashSkill`, `ValidateCandidateKey`, `Ownership` type, `EvaluateOwnership(root, e, readFile, readDir, resolvePath)` (the resolver parameter was added by the round-2 correction below)
- [x] 3a-ii.3 GREEN `engine/skills/zero_fetch_test.go`: widen `allowedImports` by `crypto/sha256`, `encoding/hex` (needed by `HashSkill`)
- [x] 3a-ii.4 REFACTOR: confirm `os/exec` and `net/*` remain absent from `engine/skills`'s reachable import set after both widenings (`encoding/json` from project-lock-format plus `crypto/sha256`/`encoding/hex` here) — made executable as `TestZeroFetchAllowlistExcludesExecAndNet`, which fails if the allowlist ever gains `os/exec`, `net`, a `net/*` package or a git-related path, and pins its exact size at 15 so a future widening cannot slip past a reviewer
- [x] 3a-ii.5 Verify: `cd engine && go vet ./... && go test ./...` clean (all 14 packages ok, `engine/skills` 0.086s), and `go run ./cmd/ skills validate --registry ../skills.registry.yaml --manifest ../overlay.manifest --source-root ../skills` reports `registry and manifest aligned (37 skills)` / `skills/ on disk matches overlay.manifest (77 files)`. Every new `EvaluateOwnership`/`HashSkill` behavior test was additionally proved non-vacuous by a temporary mutation (all reasons forced to agent-owned, hashing given a `TrimSpace`), which turned all eight RED before the mutation was reverted
- [x] 3a-ii.6 Correction group resolving the workflow review of slice 3a-ii (3 findings): SEC-1 (BLOCKER) — `EvaluateOwnership` joined each `Targets` entry onto `root` with `filepath.Join`, which cleans `..` away, so a lock entry recording `../../etc/passwd` or an absolute path read OUTSIDE the project root and could report `AgentOwned=true` for a file the project does not own; every target now passes `resolveTarget(root, target)` BEFORE any read (mirroring the R-055 containment guard in `PlanInstall`, `install.go:43-49`: clean, then require the cleaned form under cleaned-root + separator, with an explicit `..`-component check ahead of it), and a refused target yields human-owned `invalid-target <target>`. C1+F1 (WARNING) — an entry present in the lock but recording zero targets reported `not-in-lock`, conflating a malformed entry with a genuinely unregistered skill, and the guard was untested (deleting it left every test green); the zero entry (`e.ID == ""`) keeps `not-in-lock` and the ID-present-no-targets case now reports `no-targets <id>`, with the RED test that deleting the guard breaks. F2 (SUGGESTION) — the `fakeFS` doc comment claimed no test in the group touches a real filesystem while `TestEvaluateOwnership_ReadsOnlyThroughInjectedReaders` does `os.MkdirAll`/`os.WriteFile` under its own `t.TempDir()`; the comment now names that test, explains why the real writes are the point, and records that they are confined to a per-test temp dir (never `$HOME`, `.claude/skills`, `.agents/skills`, `.pi` or `skills-lock.json`). Both new reasons are documented on the `Ownership` type as additions to the design.md:210 vocabulary

- [x] 3a-ii.7 Correction group resolving the round-2 workflow review of slice 3a-ii (5 findings), the last correction round for this slice: **SEC-2 (WARNING)** — `resolveTarget` was purely lexical, so a symlinked directory component inside `root` (`root/link` -> a sibling of `root`, target `link/SKILL.md`) passed every guard and `EvaluateOwnership` hashed and reported `AgentOwned=true` for bytes outside the project, contradicting its own doc comment and leaving the package inconsistent with `install.go`'s R-055 symlink refusal. Resolution: route (a) of the two offered — `EvaluateOwnership` gains an injected `resolvePath func(string) (string, error)` and re-checks containment between the RESOLVED root and the RESOLVED target before any read (new reasons `escapes-root <target>`, `unresolved-root <root>`, `unresolved-target <target>`, and `no-resolver <id>` when no resolver is injected, so ownership fails closed instead of degrading silently to the lexical check). Route (a) over route (b) because the escape is closed HERE rather than documented and deferred to a caller that does not exist yet, and because injection preserves the function's defining property — it still performs no filesystem access of its own and needs no new import (`filepath.EvalSymlinks` would have meant real I/O inside a function whose whole contract is injected I/O). `resolveTarget` keeps its doc comment honest by stating plainly that it is LEXICAL ONLY and that resolved containment is a second step its callers owe themselves. **SEC-3 (WARNING, filed as a leak; recorded here as a MISSING GUARD)** — a target cleaning to `.` (`.`, `./`, `././`) resolved to `root` itself; the orchestrator's independent re-check established this was NOT reachable as a parent-directory listing with a real `os.ReadFile`, because reading a directory errors first (`readFile(root)` -> `ErrNotExist` -> `missing .`, `readDir` never called). Resolution: `withinRoot` is now strictly-below (`p != cleanRoot`), which refuses all three; an explicit `path.Clean(target) == "."` branch was written first, then deleted when the deletion proof showed it dead — `withinRoot` alone already turns the case red. **ROOT-1 (SUGGESTION)** — a relative or empty `root` silently flipped every verdict to human-owned via per-target `invalid-target`; a non-absolute root is now one loud `invalid-root <root>` refused before any read. **COV-1 (WARNING)** — each `resolveTarget`/ownership sub-guard now has a witness test that turns red when that guard alone is deleted, including the `..`-that-cleans-back-inside-root case (`a/../b/SKILL.md`) the scan exists for; every deletion was performed, recorded and reverted. **DOC-1 (SUGGESTION)** — design.md's reason vocabulary is amended (dated, and naming the reviews that drove it) from the original four reasons to the full eleven, alongside the amended `EvaluateOwnership` signature


## Phase 3b-i: `project-register-write` (PR 6, ~520 lines, split owner-decided 2026-09-19)

Item-30 symbols consumed: `NormalizeSlug`, `MatchCandidate`, `ParseRegistry`.

- [x] 3b-i.1 RED `engine/skills/pathguard_test.go`: extract `withinRoot` behavior from the existing `install_test.go` traversal cases (regression net, unmodified) into a focused test for the new file
- [x] 3b-i.2 GREEN `engine/skills/pathguard.go`: `withinRoot(root, p string) bool`, extracted from `PlanInstall`'s inline containment check with no behavior change; `resolvedWithinRoot` (runs `filepath.EvalSymlinks` on the deepest existing ancestor of each destination and the project root; refuses a symlinked `.claude`/`.agents` escaping the root)
- [x] 3b-i.3 GREEN `engine/skills/install.go`: `PlanInstall` calls `withinRoot` (mechanical, no behavior change); confirm `install_test.go` passes unmodified
- [x] 3b-i.4 RED `engine/skills/project_register_test.go`: registration over `t.TempDir()` writes every target (`.claude/skills/<id>/SKILL.md`, `.agents/skills/<id>/SKILL.md`) and the lock with the correct hash, mode 0644, never `.pi/`; refusals — traversal id (a parent-escaping id fixture, see design.md testing row 3b); symlinked `.claude` escaping root; foreign existing target dir not in lock; `allowed-tools`/`disable-model-invocation`/`model`/`hooks` frontmatter key present; draft path inside the project root; `\r` bytes in draft; registry match via `MatchCandidate`; any destination under `<root>/skills/` (**corrected 2026-09-20, review round 3 SPEC-2**: as first landed, this last claim was proved only by calling `underSkillsDir` directly, so the guard inside `resolveWritePath` was never reached and deleting it left the suite green; 3b-i.8 adds a `PlanProjectRegister`-level case that reaches it through a lock-recorded target. The suite now proves 30 distinct refusal cases through `PlanProjectRegister` plus 5 message-level `checkRegisterIdentity` cases); Go `projectTargets` table equals the design's fixed two rows and neither names `.pi/skills` (the rollback/rollback-of-rollback matrix moves to project-register-rollback below)
- [x] 3b-i.5 GREEN `engine/skills/project_register.go`: `ProjectTarget`, `projectTargets = []ProjectTarget{{"claude", ".claude/skills"}, {"agents", ".agents/skills"}}`, `ProjectWrite`, `ProjectPlan`, `PlanProjectRegister(in RegisterInput) (ProjectPlan, error)` implementing the validate-before-write order (§design "Validate-before-write", items 1-11)
- [x] 3b-i.5a Reuse `resolveTarget` (added in 3a-ii.6, `project_lock.go`) for every parsed `ProjectLockEntry.Targets` string consumed as a file-WRITE path here, and fold `withinRoot`/`resolveTarget` into one shared guard once 3b-i.2 extracts it. Scope correction (2026-09-20): the original deferral of review-70263a4dee1c98b7 finding R1-lock-targets-unvalidated (SUGGESTION) claimed target validation could wait for the first writer because "no writer consumes `Targets` yet". That was wrong — `EvaluateOwnership` already consumed `Targets` as a READ path in 3a-ii, and the workflow review of slice 3a-ii found the resulting traversal (SEC-1, BLOCKER). The read side is fixed in 3a-ii.6; only the write side remains open here
- [x] 3b-i.5b Provide the `resolvePath` the amended `EvaluateOwnership(root, e, readFile, readDir, resolvePath)` requires (added by 3a-ii.7 for SEC-2). Contract: resolve every symlink in the path's EXISTING ancestry and keep non-existent components literal, so an ordinary absent target still reports `missing <path>` rather than a resolution failure; error only on a genuine failure (a symlink loop, a permission denial). Bare `filepath.EvalSymlinks` does NOT satisfy this — it fails on a missing final component — so wrap it (walk to the deepest existing ancestor, resolve that, re-append the literal tail) and share one implementation with `resolvedWithinRoot` from 3b-i.2. Passing `nil` is a fail-closed `no-resolver <id>`, never a silent lexical-only check
- [x] 3b-i.6 Verify: `cd engine && go vet ./... && go test ./skills/...`

- [x] 3b-i.7 Apply notes (slice 3b-i): (1) `withinRoot` already existed in `project_lock.go:573` (added by 3a-ii.7) in the same package where 3b-i.2 plans a new `pathguard.go`, so 3b-i.2 was executed as MOVE-and-reuse, not add — declaring it twice would not compile. `project_lock.go` was touched only to delete that definition and leave a pointer comment. (2) The 3b-i.3 extraction is NOT a pure no-op: `PlanInstall`'s inline check was prefix-on-path-plus-separator, which ADMITTED a path equal to the root, while `withinRoot` is strictly-below. The tightening is deliberate, documented on `withinRoot`, and invisible to `install_test.go` (which never exercised the equality case); `install_test.go` passes unmodified. (3) design.md names `PlanProjectRegister(in RegisterInput)` but never enumerates `RegisterInput`'s fields; they are derived from validate steps 1-11 and the CLI surface, with `Stat` and `ResolvePath` injected so the planner stays pure (the `EvaluateOwnership` precedent). (4) 3b-i.5b's resolver is `resolvePathKeepingMissing` in `pathguard.go`, shared with `resolvedWithinRoot`/`resolvedWithinRootUsing` as one implementation; it uses `os.IsNotExist` rather than `errors.Is(err, fs.ErrNotExist)` because `errors` is not on the `TestZeroFetchImportAllowlist` list and no task authorized widening it. (5) 3b-i.5a is satisfied by `resolveWritePath`, the one shared write guard (lexical `resolveTarget` + resolved `resolvedWithinRootUsing` + decision (f)), applied to fresh destinations AND to every target string already recorded in the lock. (6) The mandatory Pi-trust `note:` line is defined here as `PiTrustNote`; PRINTING it on success only (never on a refusal or `--dry-run`) belongs to the CLI in phase 4.

- [x] 3b-i.8 Correction commit resolving the slice 3b-i review (round 3, 5 defect groups). **A (F2/PLAN-1/SPEC-1)**: a RELATIVE `DraftPath` bypassed the "draft must lie outside the project root" guard entirely — `filepath.Clean` does not absolutize, so the lexical `withinRoot` comparison against an absolute root was always false and the injected resolver returned an equally relative path that compared false too; a draft physically inside the root was ACCEPTED and planned. `PlanProjectRegister` now refuses a non-absolute `<draft-file>` outright, as `--project-root` already was; it is NOT absolutized against the process cwd, because the planner is pure. **B (F1)**: `resolvePathKeepingMissing` read the `ENOENT` from `filepath.EvalSymlinks` as "this component merely does not exist", so a DANGLING symlink at `<root>/.claude` was kept literal, never followed, and containment was decided on a path that only looked contained. It now distinguishes the two cases with `os.Lstat` (no allowlist widening: `os` is already on `TestZeroFetchImportAllowlist`) and reports a dangling symlink as a genuine resolution failure, while an ordinary absent component still stays literal so `EvaluateOwnership` keeps reporting `missing <path>`. **C (PLAN-2)**: `ProjectFileMode` was declared but unbound — `ProjectWrite` gains a `Mode` field carried on every planned write and on the lock write. **D (PLAN-3)**: a lock-recorded target refused for lying under `skills/` was reported as "outside the project root", which contradicts itself; `resolveWritePath` now returns the comparable `errDestUnderSkillsDir` and that caller words its own message. **E (TQ-1..TQ-5, SPEC-2)**: added the missing covering tests — the `underSkillsDir` guard reached through a lock target (TQ-1), the symlink half of the draft guard (TQ-2), `PlanInstall`'s deliberate `dst == dstRoot` tightening at the `PlanInstall` level in `pathguard_test.go` (TQ-3, `install_test.go` untouched), the nil-`Stat` fail-closed guard (TQ-4), and one case per identity branch with full distinct messages (TQ-5). TQ-5 also required a real change: the `slugRe` check now runs BEFORE the normalization check, because behind it that branch was unreachable (`NormalizeSlug`'s output always matches `slugRe`), which is why the two guards were indistinguishable. Every guard added a test for was proved load-bearing by deleting it and observing the test turn red.

- [x] 3b-i.9 Correction commit resolving the slice 3b-i review (round 2 of the corrected slice, 1 security finding + 4 coverage findings). **SEC-1**: decision (f) was enforced LEXICALLY only — `underSkillsDir` inspects the repo-relative string and the resolved containment check only proves a destination is INSIDE the root, never that it is outside `<root>/skills` — so with `<root>/.claude/skills` a symlink to `<root>/skills` the registration was ACCEPTED and its two writes would have landed physically in the project's own source tree, exactly what "the overlay's source tree is never a registration destination" forbids. `resolveWritePath` now re-applies decision (f) to the RESOLVED destination through the same injected resolver, keeping the lexical pass as the cheap first check, and refuses with a message naming the resolved skills/ tree (`errDestResolvesUnderSkillsDir`, distinct from `errDestUnderSkillsDir` so the lock-entry caller can word each honestly); because the resolved helper is strictly-below, a destination resolving to `<root>/skills` ITSELF is refused by an explicit equality check as well. Three RED cases cover it: symlinked `.claude/skills`, symlinked `.agents/skills`, and a lock-recorded target reaching `skills/` through a symlink, plus the equality case through a lock target. **COV-1**: the `!os.IsNotExist(err)` genuine-failure branch of `resolvePathKeepingMissing` had no witness (the symlink-loop case does not reach it — the `Lstat` probe catches the loop with that branch deleted); an unreadable mode-0000 parent, which defeats `EvalSymlinks` and `Lstat` alike, does, and the guard-level half proves that deleting it turns a containment REFUSAL into `inside=true` for a symlink escaping the root. Both cases skip (never fail) when running as root. **COV-2**: the lexical half of the shared write guard (`resolveTarget` + "escapes the project root") is now witnessed by a lock-recorded target whose `..` segments normalize back INSIDE the root — the one shape the resolved check cannot see. **COV-3**: `underSkillsDir`'s `clean == "skills"` disjunct is witnessed by a lock-recorded target equal to exactly `skills`; the two decision-(f) refusals also assert each other's wording is ABSENT, so the lexical pass can no longer be deleted behind the new resolved one. **COV-4**: the root-resolution error branch of `resolvedWithinRootUsing` is witnessed by an injected resolver that fails on the root only (its error must propagate, `errors.Is`), and the fail-open shape it guarded — `withinRoot("", p)` is true for every absolute path — is closed for good by an explicit empty-resolution refusal that no longer depends on a zero value. **COV-5**: the dead `draft == "" || draft == "."` branch is DELETED (unreachable since the absolute-path refusal landed: `filepath.Clean("")` is `"."`, and neither is absolute); of the remaining survivors, the frontmatter blank-line skip and the walk's `parent == cur` stop are documented in code as defence-in-depth shadowed by, respectively, the `splitKeyValue` check and the `EvalSymlinks` call (`"/"` and `"."` always resolve, so nothing can witness it, and it is the loop's termination proof), while the lexical `underSkillsDir` pass gained the witness described under COV-3. Every guard covered here was proved load-bearing by deleting it and observing a named test turn red.

- [x] 3b-i.10 Witness test for the destination-inspection guard (review round 2, COV-A): a `Stat` failure that is not "does not exist" (a permission denial) must refuse the plan instead of being read as "absent", which would fail open. Proved by mutation: neutralising the branch makes the test report `got 2 writes`. Remaining round-2 advisories (ALIAS-1 two fixed targets resolving to one physical directory; COV-B the path-resolution error branch of resolvedWithinRootUsing; EFF-1 duplicate resolution work in the decision-(f) block) carry to slice 3b-ii.

## Phase 3b-ii: `project-register-rollback` (PR 7, ~570 lines, split owner-decided 2026-09-19)

Item-30 symbols consumed: none directly — this sub-slice covers stage/commit/rollback mechanics over the plan produced by project-register-write.

- [x] 3b-ii.1 RED `engine/skills/project_register_test.go`: rollback leaves the pre-run tree byte-identical with an injected failure at each rename index and during staging (tree snapshot before/after)
- [x] 3b-ii.2 GREEN `engine/skills/project_register.go`: `ExecuteProjectPlan(p ProjectPlan, fsys projectFS, stdout, stderr io.Writer) error` implementing stage/commit/rollback over the injected `projectFS` interface
- [x] 3b-ii.3 RED `engine/skills/project_register_test.go`: using the injected `projectFS` fake, make its remove call fail during rollback itself (a rollback-of-rollback failure), asserting the process prints `error: rollback incomplete: <rel-path>` and exits 1, distinct from the ordinary rollback-succeeds cases above (carries forward 3b.5a / the reliability-review rollback-of-rollback addition)
- [x] 3b-ii.4 REFACTOR: confirm the rollback path removes renamed new files, leftover temps, and only-if-empty created directories (deepest first) on every injected failure point, and prints `error: rollback incomplete: <rel-path>` + exit 1 on a rollback failure, as proven above
- [x] 3b-ii.5 Verify: `cd engine && go vet ./... && go test ./skills/...`

- [x] 3b-ii.6 ALIAS-1 (carried from 3b-i.10): two FIXED targets that resolve to the SAME physical directory. Decision: **refuse the registration**, in the PLANNER (`PlanProjectRegister`, where every destination is already resolved), with the executor re-applying it as the check-then-act half. Collapsing to one write was rejected: the lock would record two targets for one file, so `EvaluateOwnership` would read the same bytes twice and a human deleting "one" copy would appear to have deleted both; and rollback's headline guarantee — the pre-run tree comes back byte-identical — becomes conditionally false, because the second write silently overwrites the first and only one of the two removals can succeed. Witnessed with a REAL symlink (`<root>/.agents` -> `<root>/.claude`) at both levels, each preceded by a precondition proving the un-aliased input plans cleanly
- [x] 3b-ii.7 COV-B (carried from 3b-i.10): the DESTINATION half of `resolvedWithinRootUsing`'s resolution-error branch (`pathguard.go`, `resolve(p)`) now has its own witness — an injected resolver that fails on the destination only, asserting the error propagates (`errors.Is`) and containment is never reported. Proved load-bearing by deleting the branch (the test turns red)
- [x] 3b-ii.8 EFF-1 (carried from 3b-i.10): the decision-(f) block in `resolveWritePath` resolved the same two paths four times (`resolvedWithinRootUsing` resolves both its arguments, then the equality check resolved both again). Each side is now resolved ONCE and both halves are derived from the results — strictly-below containment OR explicit equality with `skills/` itself — so review round 2 SEC-1 and round 3 PLAN-3 semantics are preserved exactly; the empty-resolution refusal COV-4 added inside the helper is re-stated explicitly here rather than inherited. No test counts resolver invocations, so the call-count change breaks nothing. The resolved destination is now returned to the caller, which is also what makes ALIAS-1 free of any extra resolution
- [x] 3b-ii.9 Portability (carried from 3b-i.10): the mode-0000 cases in `pathguard_test.go` skipped only for `os.Geteuid() == 0`, but a non-root euid does not imply mode 0000 denies — `CAP_DAC_OVERRIDE`, several container/CI users and mode-ignoring filesystems all read the directory anyway, where the first case failed spuriously and the second passed vacuously. Replaced with a behavioural probe (`chmodDeniesReading`: chmod, then actually attempt the read, and skip only when it succeeded). This is a test-only CI-robustness change with no observable RED on this machine, where mode 0000 does deny
- [x] 3b-ii.10 Apply notes (slice 3b-ii): (1) design.md names the `projectFS` interface but never enumerates its methods; the method set (`Stat`, `ReadDir`, `MkdirAll`, `WriteTemp`, `Rename`, `Remove`, `ResolvePath`) is derived from the stage/commit/rollback prose and documented as such on the type. `ReadDir` exists so "remove created directories only if empty" is an explicit check rather than an un-inspectable `ENOTEMPTY` this package cannot classify (`errors` is not on the zero-fetch allowlist). (2) Precedent matched: `writeFileAtomic` (`lifecycle.go`) for the temp pattern (`.tmp-skills-*` in the destination's own directory, write -> sync -> close, `fmt.Errorf("<fn>: <step>: %w", err)`), the AddCore/RemoveCore stage-all-temps-then-rename-in-order choreography with removal of leftovers on failure, and `cmd/main.go`'s `atomicWriteFile` for the `os.Chmod(tmp, perm)` step `lifecycle.go` omits — without it `os.CreateTemp`'s 0600 would survive the rename and `ProjectFileMode` would never reach disk. `ExecuteInstall`'s `RemoveAll`-then-copy was deliberately NOT used as a model. (3) design.md fixes `ExecuteProjectPlan`'s signature and it carries no project root, so the root is derived from the LOCK write (`Abs` minus `Rel`), which also refuses a zero or hand-built plan before anything is touched. (4) Rollback sweeps every PLANNED write, not only the renames observed to succeed, because a rename that reports an error may still have landed; restoring or removing an untouched destination is a no-op. That is also what makes the pre-existing-lock RESTORE reachable at all, since the lock is renamed last and nothing follows it — the test drives it with a rename that lands and then reports failure. (5) `ExecuteProjectPlan` returns `ErrRollbackIncomplete` (wrapped) after printing `error: rollback incomplete: <rel-path>` per unrecovered path; mapping a non-nil return to exit 1 belongs to the CLI in phase 4, so "exits 1" is asserted here as a distinguishable error plus the exact stderr line. (6) Directory mode is unlegislated by design.md; `projectDirMode` is 0755, documented at its declaration. (7) `wrote:` lines are printed only after the whole set is committed, so a line a later rollback erases is never emitted. (8) No new imports beyond `io` and `sort`, both already on `TestZeroFetchImportAllowlist`. (9) Every new guard was proved load-bearing by deleting it and observing a named test turn red
- [x] 3b-ii.11 Correction round (review round 4, slice 3b-ii): the slice's headline guarantee — after a failed run the pre-run tree is byte-identical, **including file modes** — was false, and thirteen confirmed findings deduplicated to six defect groups. **D1** rollback restored through the planned `Mode`, which is always `ProjectFileMode`, so a project keeping its lock at 0600 got it back at 0644 after a failed run; and because rollback swept every PLANNED write it also rewrote destinations this run never renamed over, giving them a new inode and the planned mode. The destination's REAL mode is now read immediately before each rename (`preMode`), restore uses it, and `attempted` bounds the sweep to the destinations this run actually reached — note (4) of 3b-ii.10 above is thereby narrowed from "every planned write" to "every ATTEMPTED write", which still covers the rename-that-lands-and-reports-failure case it was written for. Witnessed by a pre-existing lock at 0600 (the existing byte-identical test wrote it at 0644 and was therefore mode-blind) plus an inode assertion. **D2** `s.created` was appended only after `MkdirAll` returned nil, but `os.MkdirAll` creates the ancestors it can before failing deeper and never reports which; those leaked forever. Directories are now created ONE LEVEL AT A TIME and recorded as each succeeds, so "created" and "recorded" are the same event; witnessed with a fake that models `os.MkdirAll`'s partial creation, since a fake that refuses outright leaves a clean tree the buggy code also leaves clean. **D3** restore-vs-remove was decided purely from the plan-time `Backup`, and every SKILL.md write has `Backup == nil`, so a destination that appeared between plan and execution was OVERWRITTEN on success and DELETED by rollback. Decision: **refuse**, not back-up-and-restore — the planner already refuses a destination that exists and is not in the lock ("foreign skill"), and a destination appearing late is that same fact arriving late; answering a race more permissively than the look-first path would mean overwriting on a race exactly what is refused when checked in time, and refusing costs nothing because nothing has been written yet. The mirror case (planner found it present, it is now gone) is refused for the same reason. **D4** the executor's re-proof re-established root containment and the alias rule but dropped decision (f), so a `.claude/skills` that became a symlink into the project's own source tree after planning was written through; decision (f) is now re-applied to both the repo-relative string and the RESOLVED destination, witnessed with a real symlink created after the plan was built. **D5** `(*projectStager).rel` sat at 0.0% coverage, so the repo-relative pointer in every `error: rollback incomplete: <rel-path>` line was unproven for leftover temps and created directories; both branches are now covered with exact-line assertions (0.0% -> 100.0%). This is test-only: no honest RED exists, because the code was already correct. **D6** `failAt`'s doc comment claimed to fail the n-th matching call but kept no counter; the comment is corrected rather than a counter added, because no test needs one (`failAfterRename` owns its own latch). No new non-test imports; `syscall` and `path` are test-only additions for the inode assertion and repo-relative directory pointer. Each of the five behavioural guards was proved load-bearing by reverting it and observing a named test turn red.

- [x] 3b-ii.12 Witness test for the executor's LEXICAL decision-(f) pre-pass (round-2 review, coverage F1), plus a correction to two claims made about it. The review filed the guard as uncovered AND fail-open; the orchestrator verified both halves: uncovered is TRUE (disabling it left the suite green, contradicting the fix commit's mutation table, which claimed a red test), but fail-open is FALSE (the resolved check below it still refuses the same write, with the wording "resolves into the project's own skills/ tree" instead of "lies under skills/"). The new test pins the lexical wording, so disabling the pre-pass now turns it red. Carried to slice 4: stale-backup-overwrites-a-concurrent-edit on a failed run; the mode==0 sentinel conflating "unrecorded" with "really 0000"; Perm() dropping setuid/setgid/sticky on restore; the Stat-before-rename failure branch and the generic stat-error branch of the re-check both uncovered; and a rollback doc comment that contradicts its own body.

## Phase 4: `project-register-cli` (PR 8, ~310 lines, size:exception owner-granted 2026-09-19)

Item-30 symbols consumed: sec 2 record block (`Registered` line wiring in the procedure, prose only).

- [x] 4.0 Carry-forward fixes from review-c4c6f452b3ecc485 (owner-accepted, apply-time): (1) doc comment on the unknown-flag test naming the exact flag-rejection behavior it protects; (2) doc comment on `generatedBlockMarkerLocation` clarifying its shared use across the numeric-source-drift test; (3) support a `--` end-of-options escape so a dash-prefixed draft path is not misparsed as a flag; (4) tests for an unknown flag given before the path argument, and for a `--rules`-equivalent flag given together with an extra positional argument
- [x] 4.1 RED `engine/skills/project_cli_test.go`: `project-register --project-root <abs> --candidate <key> [--dry-run] <draft>` flag parsing; missing `--project-root` exits 1 with nothing written; relative `--project-root` refused; `--dry-run` prints `plan: <rel>` lines and writes nothing; a successful non-dry-run write prints `wrote: <rel>`, `sha256: <hex>`, `revision: <n>`, and the exact `note: Pi loads .agents/skills only after the project is trusted; Pi may prompt once for project trust.` line; the `note:` line is present only on a successful write and absent on refusal or `--dry-run`; `skills-lock.json` bytes are unchanged in every case; wrapper-injected trailing flags tolerated
- [x] 4.2 GREEN `engine/skills/project_cli.go`: `RenderProjectRegisterCore` implementing `project-register` and `--dry-run`, printing the Pi trust `note:` line after a successful write
- [x] 4.3 GREEN `engine/skills/skills.go`, `engine/cmd/main.go`: dispatch `project-register`; usage strings
- [x] 4.4 GREEN `skills/_shared/procedural-candidate-detection.md`: section 11 — registration and commit procedure (the 10-step agent git sequence: toplevel check, HEAD/in-progress-op check, empty-index check, `--dry-run` plan, `check-ignore`, real run, `add` + staged-set verification, `commit` with the exact conventional messages, `project-status` ownership confirmation, `rev-parse --short HEAD` recorded to `Registered`/`History`); crash-window recovery step (delete only `??`-and-hash-verified paths); acceptance checklist for `sdd-verify`
- [x] 4.5 RED `engine/skills/procedural_candidate_contract_test.go`: extend to assert section 11 appears verbatim (the 10 steps, the three exact commit messages, the crash-recovery rule)
- [x] 4.6 REFACTOR: confirm the CLI's printed paths are repo-relative with slashes and usable directly as a git pathspec
- [x] 4.7 Verify: `cd engine && go vet ./... && go test ./skills/... ./cmd/...`
- [x] 4.8 Acceptance checklist (non-Go, executed during `sdd-verify`, temp git repo with real Engram records): clean repo → exactly one commit whose changed paths equal the wrote set; unrelated staged file → refusal, index untouched; ignored target → refusal; detached HEAD → refusal; `git revert` restores files and lock; `History` only grows across draft→register; a staged-set mismatch after `git add -- <wrote paths>` (an unexpected path staged alongside them) → refusal, staged set unstaged via `git restore --staged`; a merge or rebase in progress (`MERGE_HEAD` or `rebase-merge`/`rebase-apply` present) → refusal; running the procedure from a subdirectory of the project root or from a nested repository root → resolved or refused per the design's `rev-parse --show-toplevel` check, never silently registering against the wrong root; a commit hook that rewrites the committed bytes so the post-commit hash differs from what was written → reported as human-owned by `project-status`, not auto-corrected; a second registration (for a different skill) that fails after `wrote:`/`sha256:` output but before its own commit → the crash-recovery step deletes only that run's untracked, hash-verified SKILL.md files and restores the lock file from `HEAD`, and a prior skill's lock entry and committed files survive unchanged

- [x] 4.9 Apply notes (slice 4): (1) `RenderProjectRegisterCore(args, readFile, statFile, resolvePath, fsys, stdout, stderr, exit)` follows the package's Core shape (`RenderLintCore` precedent) with the extra dependencies appended as injected seams before the writers; `fsys` is the unexported `projectFS`, which is deliberate — every production caller reaches the core through `SkillsCore`, and tests inject `osProjectFS{}` over `t.TempDir()`. (2) Read order is registry, draft, lock. An unreadable or unparseable registry refuses (design.md "Identity": fail-closed, because registering while `MatchCandidate` cannot run is exactly the shadowing the check prevents); an absent project lock is the normal first-registration case (`LockExists: false`), while any OTHER lock read error refuses rather than silently replacing the lock with an empty one and dropping every already-registered skill. (3) `--project-root` is checked for absoluteness in the CLI, BEFORE the lock path is joined against it, so a relative root can never aim a read or a write at a cwd-derived location; the planner re-checks it. (4) `--dry-run` prints `plan: <rel>` in `projectCommitOrder` (targets, lock last) — the same order and the same strings the real run prints as `wrote:`, which is what lets `git check-ignore` and `git add` see one pathspec set; a dedicated test asserts the two sets are identical, repo-relative, slash-separated and each resolves to a real file under the root. (5) The Pi trust `note:` line is printed by the CLI as the LAST line of a successful run only; `--dry-run` and every refusal print nothing to stdout at all, because the agent feeds stdout straight to `git add --`. (6) Task 4.0 item (3) (`--` end-of-options) and item (4) (unknown flag before the path; a flag together with an extra positional) were applied to BOTH parsers: `RenderLintCore`, which the review filed them against, and the new `project-register` parser, which takes a positional `<draft-file>` and would otherwise have shipped the identical dash-prefixed-path defect on day one. Items (1) and (2) are doc comments and had no RED, as did the two lint tests that pin already-correct behaviour; only the two `--` tests were RED. (7) No new imports: `bytes`, `fmt`, `io`, `io/fs`, `os`, `path/filepath` and `strings` are all already on `TestZeroFetchImportAllowlist`. (8) Section 11's ten steps, three commit messages, refusal table and crash-recovery rule are pinned non-vacuously: the assertions run against a slice bounded by section 11's heading and the registration acceptance checklist's heading, the ten step markers are walked with a moving cursor (so a reorder fails), and the step count and the refusal-row count are pinned at 10 and 6. Proved load-bearing three ways: deleting section 11 turns 9 subtests red, dropping one step turns the order walk AND the count red, and swapping two step commands turns the order walk red
- [x] 4.10 Re-deferred to slice 7a-i `project-retire` (carried from 3b-ii.12, NOT fixed here): the six register-engine items all live in `engine/skills/project_register.go`, `project_register_test.go` and `pathguard.go` — files phase 4 does not otherwise touch — and slice 4 already carries an owner-granted `size:exception`. Folding them in would re-open a merged slice's code inside a CLI slice and mix two file scopes in one review candidate. 7a-i is the next slice that reuses the same plan/execute/rollback mechanics (for retirement), so it is the natural owner; phase 5 (`global-promotion`, `lifecycle.go`) never touches this engine. The six, with their verdicts: (a) **stale backup overwrites a concurrent edit** — `ProjectWrite.Backup` is captured at plan time and `checkProjectDestinations` re-checks existence but not content, so an edit landing after the re-check and before a failure is overwritten with plan-time bytes; behavioural, owner 7a-i. The WINDOW itself is documented in slice 4's own section 11 crash-window prose, which is where an operator meets it. (b) **the `mode==0` sentinel** conflates "never recorded" with "really 0000"; defensive-only, currently unreachable, owner 7a-i. (c) **`Perm()` drops setuid/setgid/sticky on restore**, so the rollback slice's headline "byte-identical including modes" is still narrowly false; behavioural, owner 7a-i. (d) the **Stat-before-rename failure branch** and (e) the **generic stat-error branch of the executor re-check** are both uncovered; test-only, cheap via the existing `fakeProjectFS`, owner 7a-i. (f) the **restore branch of rollback-of-rollback** (`Backup != nil`, both failure exits) is uncovered, and is the highest-consequence of the three coverage gaps because the only write with `Backup != nil` is a pre-existing lock; test-only, owner 7a-i. Also carried: the `(*projectStager).rollback` doc comment still says it "walks the PLANNED writes" while round 4 D1 narrowed the body to ATTEMPTED writes — a one-line doc fix that must travel with the engine items so the comment and the code land corrected together

## Phase 5: `global-promotion` (PR 9, ~200 lines, size:exception owner-granted 2026-09-19)

Item-30 symbols consumed: sec 2 record block (`Promoted` line, prose only).

- [x] 5.1 RED `engine/skills/lifecycle_test.go`: extended `AddCore` coverage with a missing-`metadata.version` hard-lint fixture that exits non-zero, names the finding, and preserves registry/manifest bytes; added a warnings-only fixture that still registers; updated AddCore fixtures to be lint-clean and retained the existing SC-65 through SC-68 assertions. RED evidence: `cd engine && go test ./skills/...` failed only at `TestAddCoreRejectsHardLintErrorBeforeWrites` because the gate was absent.
- [x] 5.2 GREEN `engine/skills/lifecycle.go`: `AddCore` now reads `<src>/<id>/SKILL.md` after the existence check, runs `LintSkillFile`, prints each hard finding, and exits before registry serialization or manifest processing; warnings do not block. GREEN evidence: `cd engine && go test ./skills/...` passed.
- [x] 5.3 GREEN `skills/_shared/procedural-candidate-detection.md`: added section 12 documenting human-only promotion through `engine skills add`/`AddCore`, silence-is-not-consent, the exact `Promoted` hash line, and append-only `History` wiring.
- [x] 5.4 REFACTOR: the hard-lint refusal returns before `Serialize`, manifest read/append, or either atomic write; the byte-preservation regression test passes.
- [x] 5.5 Verify: `cd engine && go vet ./... && go test ./...` and focused `cd engine && go test ./skills/...` passed.

## Phase 6: `revision` (PR 10, ~300 lines, size:exception owner-granted 2026-09-19)

Item-30 symbols consumed: sec 3 occurrence rules, sec 4 emission table (new rows, prose only).

- [x] 6.1 RED `engine/skills/project_register_test.go`, `project_cli_test.go` (extend): `project-revise` refused per each `EvaluateOwnership` reason (`hash-mismatch`, `missing`, `extra-entry`) with the reason named in output and nothing written; a hash-matching revision bumps `revision`, recomputes `sha256`, and restores backup bytes on an injected mid-revision failure (rollback); `project-status` reports `owner:agent|human (<reason>)`
- [x] 6.2 GREEN `engine/skills/project_register.go`: `project-revise` planning/execution path — ownership gate via `EvaluateOwnership`, revision-number bump, backup-capture-then-restore-on-failure (temp+rename pattern, same as new registration)
- [x] 6.3 GREEN `engine/skills/project_cli.go`: `project-status` (ownership reporting) and `project-revise` CLI wiring
- [x] 6.4 GREEN `skills/_shared/procedural-candidate-detection.md`: section 13 — revision trigger (`OccurrencesSincePromotion >= 2`, derived not stored), new emission-table rows for post-registration occurrences, the "qualifying occurrence" rule (failure-recovery recurrence, or repeated-success where the instruction was missing/wrong/rediscovered — simply following the skill does not count)
- [x] 6.5 REFACTOR: confirm `project-revise` reuses the exact same stamp→lint→hash→write ordering as new registration (no divergent code path)
- [x] 6.6 Verify: `cd engine && go vet ./... && go test ./skills/...`

## Phase 7a-i: `retirement-engine-core` (PR 11, ~490 lines, split owner-decided 2026-09-19)

Item-30 symbols consumed: `MatchCandidate` (for `AbsorbedInto` existence verification), sec 6.

- [x] 7a-i.1 RED `engine/skills/project_register_test.go` (extend): `project-retire` removes target files and the lock entry in one operation for an agent-owned skill; refused for a human-owned skill (reason named); `AbsorbedInto` write succeeds only when the named id exists (global: `MatchCandidate` against the overlay registry as an existence lookup, not coverage proof; project: an entry in the project lock) and is refused with the unverified target named otherwise; a failure injected mid-retirement (via the `projectFS` fake) triggers rollback of the partial delete, leaving the pre-retirement tree byte-identical, and a failure during that rollback itself prints `error: rollback incomplete: <rel-path>` and exits 1
- [x] 7a-i.2 GREEN `engine/skills/project_register.go`: `project-retire` planning/execution (delete targets + lock entry, ownership-gated); `AbsorbedInto` existence verification helper
- [x] 7a-i.3 Verify: `cd engine && go vet ./... && go test ./skills/...`

## Phase 7a-ii: `retirement-cli-and-docs` (PR 12, ~415 lines, split owner-decided 2026-09-19)

Item-30 symbols consumed: sec 6 (`project-status` supersession reporting via `MatchCandidate`).

- [x] 7a-ii.1 RED `engine/skills/project_cli_test.go` (extend): `project-retire` CLI dispatch over the core planning/execution from retirement-engine-core; `project-status` reports `superseded-by:<path>` when `MatchCandidate(registry, id)` or `MatchCandidate(registry, <last candidate slug>)` matches a global skill
- [x] 7a-ii.2 GREEN `engine/skills/project_cli.go`: `project-retire` CLI wiring; `project-status` supersession reporting via `MatchCandidate`
- [x] 7a-ii.3 GREEN `skills/_shared/procedural-candidate-detection.md`: section 14 — retirement decision procedure, `RetirementReason` vocabulary (`stale-reference | superseded | absorbed | promoted | quiet | human-request`), `AbsorbedInto` verification rule, global-tier `RemoveCore` path (human, unmodified)
- [x] 7a-ii.4 REFACTOR: confirm `project-retire` never invokes removal automatically from a detector result — it is always a separate, explicitly invoked command
- [x] 7a-ii.5 Verify: `cd engine && go vet ./... && go test ./skills/...`

## Phase 7b: `retirement-detector` (PR 13, ~350 lines, size:exception owner-granted 2026-09-19)

Item-30 symbols consumed: sec 2 field names (`LastObserved`, `Status`), read-only.

- [x] 7b.1 RED `longterm-mem/internal/staleness/staleness_test.go` (extend): `ReferencedPaths(text)` extracts repo-shaped tokens from inline code spans and fenced blocks using the existing strict `filePath` pattern; `ClassifyPaths(repoRoot, paths)` reuses `indexTree`/`repohistory.Inspect` to classify each as removed-in-history vs still-present vs moved; `Detect`'s existing behavior and tests are unchanged
- [x] 7b.2 GREEN `longterm-mem/internal/staleness/staleness.go`: export `ReferencedPaths`, `ClassifyPaths`, sitting beside `Detect` and sharing its private helpers, with no change to `Detect` itself
- [x] 7b.3 RED `longterm-mem/internal/skillstale/skillstale_test.go` (new), fixture `longterm-mem/internal/skillstale/testdata/procedural-skills.lock.json`: detector over a temp git repo + fixture Engram DB — a skill naming a git-removed path is flagged `REMOVED <path> (by <commit>)` regardless of ordering; a renamed path yields `MOVED <path> -> <new>` only, never `REMOVED`; a clean skill (all paths present, all commands resolvable, `LastObserved` recent, no supersession) is not flagged; an unresolvable fenced command's first word yields `UNRESOLVED-COMMAND <name>` (resolved via `os.Stat` scan of `$PATH`, no `os/exec`); `LastObserved` older than 180 days yields `QUIET since <LastObserved>`; the detector performs zero file/record mutation (tree snapshot identical before/after, Engram store opened read-only); the fixture lock parses and `engine/skills/project_lock_test.go`'s `SerializeProjectLock` reproduces the shared fixture byte-for-byte (cross-module pinning, verified in 7b.6)
- [x] 7b.4 GREEN `longterm-mem/internal/skillstale/skillstale.go`: the detector over the lock, the first target SKILL.md of each entry, `staleness.ReferencedPaths`/`ClassifyPaths`, `os.Stat`-based `$PATH` command resolution, and `engram.Store.ListObservations` filtered by `TopicKey` for `LastObserved`/`Status` (read-only)
- [x] 7b.5 GREEN `longterm-mem/cmd/longterm-mem/cmd_skills_stale.go`, `main.go`: `longterm-mem skills-stale --project-root <abs> [--project <P>]`, report-only subcommand
- [x] 7b.6 GREEN `engine/skills/project_lock_test.go`: add the pinning test asserting `SerializeProjectLock` reproduces `longterm-mem/internal/skillstale/testdata/procedural-skills.lock.json` byte-for-byte
- [x] 7b.7 REFACTOR: confirm no new `os/exec`-importing package was introduced in `longterm-mem` (the module's existing `allowedExecImporters`/equivalent guard, if any, is unchanged) and that `skillstale` and `cmd_skills_stale.go` perform no write to the lock, the SKILL.md files, or Engram
- [x] 7b.8 Verify: `cd longterm-mem && go vet ./... && go test ./internal/staleness/... ./internal/skillstale/... ./cmd/...`

## Phase Final: Full Verification (after PR 13 merges)

- [ ] F.1 `cd engine && go vet ./... && go test ./...`
- [ ] F.2 `cd longterm-mem && go vet ./... && go test ./...`
- [ ] F.3 Confirm no file was written under `skills/` by any project-tier code path outside `AddCore`'s own tests (`git diff --stat` review across all 13 merged PRs)
- [ ] F.4 Confirm no test in either module touched a live `.claude/skills/`, `.agents/skills/`, `.pi/`, `skills-lock.json`, the user's home directory, or spawned a live runtime binary (temp-dir-only rule)
- [ ] F.5 Run the full acceptance checklist from sections 11 (Phase 4), and the sdd-verify checklists implied by Phases 2, 6, 7a-i, 7a-ii against real Engram records and a temp git repo; record results honestly, including any scenario that could not be exercised (e.g. the rollback-failure print path, or the Codex smoke test if `UNVERIFIED`)
- [ ] F.6 Confirm every Success Criteria checkbox in `proposal.md` is satisfied or explicitly recorded as a known gap (in particular the Codex-support claim, gated strictly by the recorded smoke-test verdict)
