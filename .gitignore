*.swp
*.swo
*~
.DS_Store

# Compiled TUI binaries (Go source is committed; binaries are not)
bin/overlay-tui
tui/tui

# Engram runtime (project resolution cache; not versioned)
.engram/

# Skill registry — machine-generated by the overlay hook on every prompt.
# Tracking it caused every git pull/checkout to be blocked by local churn.
# Regenerated automatically; not versioned.
.atl/skill-registry.md
.atl/.skill-registry.cache.json

# Go coverage artifacts
coverage.out
coverage.html
# Local AI runtime state
.atl/
